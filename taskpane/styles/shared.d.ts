/**
 * A Word task pane is ~320px wide. Flex children default to `min-width: auto`,
 * which stops them shrinking below their content and pushes the pane into
 * horizontal scroll — every flexible cell here sets `minWidth: 0` to prevent it.
 */
export declare const useSharedStyles: () => Record<"page" | "block" | "row" | "form" | "field" | "fieldFull" | "sectionTitle" | "hint" | "actions" | "actionsEnd" | "wrapText" | "colorInput", string>;
//# sourceMappingURL=shared.d.ts.map